{
    "Version": "2012-10-17",
    "Statement": [
        {
            "Effect": "Allow",
            "Principal": {
                "AWS": "${website_cdn_iam_arn}"
            },
            "Action": [
                "s3:GetObject"
            ],
            "Resource": "${bucket_arn}/*"
        }
    ]
}