{
    "Version": "2012-10-17",
    "Id": "PolicyForCloudFrontPrivateContent",
    "Statement": [
        {
            "Effect": "Allow",
            "Principal": {
                "AWS": "${cdn_arn}"
            },
            "Action": [
                "s3:GetObject"
            ],
            "Resource": "${bucket_arn}/*"
        }
    ]
}